{
  "name": "jailbreak-platformer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc && vite build",
    "test": "vitest run",
    "lint": "tsc --noEmit",
    "smoke:browser": "node scripts/browser-smoke.mjs"
  },
  "dependencies": {
    "phaser": "^3.90.0",
    "vite": "^7.0.0"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
